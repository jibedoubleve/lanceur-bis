<UserControl x:Class="Lanceur.Ui.WPF.Views.Controls.AdditionalParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             MinWidth="800">
    <StackPanel>
        <!--  NAME  -->
        <Label Margin="6,0,6,3" Target="{Binding ElementName=TbAliasName}">Name</Label>
        <TextBox Text="{Binding Name}" Margin="0,0,0,10" x:Name="TbAliasName"/>
        <!--  PARAMETER  -->
        <Label Margin="6,0,6,3" Target="{Binding ElementName=TbAliasParameter}">Parameter</Label>
        <TextBox Text="{Binding Parameter}" x:Name="TbAliasParameter"/>
    </StackPanel>
</UserControl>