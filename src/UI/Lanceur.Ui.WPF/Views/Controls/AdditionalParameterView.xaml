<UserControl x:Class="Lanceur.Ui.WPF.Views.Controls.AdditionalParameterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             MinWidth="800">
    <StackPanel>
        <!--  NAME  -->
        <Label Margin="6,0,6,3" Target="{Binding ElementName=TbAliasName}">Name</Label>
        <TextBox Text="{Binding Name}" Margin="0,0,0,10" />
        <!--  PARAMETER  -->
        <Label Margin="6,0,6,3" Target="{Binding ElementName=TbAliasName}">Parameter</Label>
        <TextBox Text="{Binding Parameter}" />
    </StackPanel>
</UserControl>