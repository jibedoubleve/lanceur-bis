<Page
    x:Class="Lanceur.Ui.WPF.Views.Pages.LuaEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Lua Editor"
    d:Height="800"
    d:Width="900"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="100" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="100" />
        </Grid.RowDefinitions>

        <!--  HEADER WITH BACK AND SAVE BUTTONS  -->
        <ui:TextBlock
            Grid.Row="0"
            Margin="10,10,10,5"
            VerticalAlignment="Center"
            FontTypography="Subtitle"
            Text="Lua Script Editor" />

        <!--  MENU WITH DRY RUN  -->
        <Menu Grid.Row="1" Margin="5,0,5,0">
            <MenuItem
                Click="OnClickBack"
                Header="Back"
                Icon="{ui:SymbolIcon ArrowLeft24}"
                ToolTip="Go back to Keywords" />
            <MenuItem Command="{Binding DryRunCommand}" Header="Dry run">
                <MenuItem.Icon>
                    <ui:SymbolIcon
                        Filled="True"
                        Foreground="Green"
                        Symbol="Play24" />
                </MenuItem.Icon>
            </MenuItem>


            <MenuItem
                Grid.Column="2"
                Click="OnClickSave"
                Header="Apply changes"
                Icon="{ui:SymbolIcon Save24}"
                ToolTip="Save changes and go back" />

        </Menu>

        <!--  INPUT FIELDS  -->
        <Grid Grid.Row="2" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <Label
                    Margin="5,0,0,0"
                    Content="Parameters:"
                    Target="{Binding ElementName=TbParameters}" />
                <ui:TextBox
                    x:Name="TbParameters"
                    Margin="5,0,5,5"
                    Text="{Binding Parameters, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <Label
                    Margin="5,0,0,0"
                    Content="File name:"
                    Target="{Binding ElementName=TbFileName}" />
                <ui:TextBox
                    x:Name="TbFileName"
                    Margin="5,0,5,5"
                    Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </Grid>

        <!--  LUA EDITOR  -->
        <Canvas x:Name="EditorCanvas" Grid.Row="3" Margin="5">
            <avalonedit:TextEditor
                x:Name="LuaEditor"
                Width="{Binding ActualWidth, ElementName=EditorCanvas}"
                Height="{Binding ActualHeight, ElementName=EditorCanvas}"
                Background="{DynamicResource ApplicationBackgroundBrush}"
                FontFamily="Consolas"
                FontSize="10pt"
                Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                KeyUp="OnKeyUp"
                ShowLineNumbers="True"
                SyntaxHighlighting="Lua"
                VerticalScrollBarVisibility="Visible" />
        </Canvas>

        <GridSplitter
            Grid.Row="4"
            Height="5"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            Background="{DynamicResource ControlStrokeColorDefaultBrush}" />

        <!--  OUTPUT TABS  -->
        <TabControl
            x:Name="OutputTabControl"
            Grid.Row="5"
            Margin="5">
            <TabItem Header="Output">
                <Grid VerticalAlignment="Stretch" Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <Label
                        x:Name="ScriptOutput"
                        Margin="5"
                        VerticalAlignment="Stretch"
                        Content="{Binding Output}"
                        FontFamily="Consolas"
                        FontSize="10pt"
                        Visibility="{Binding HasError, Converter={StaticResource InvertBooleanToVisibilityConverter}}" />
                    <Label
                        x:Name="ScriptErrorOutput"
                        Margin="5"
                        VerticalAlignment="Stretch"
                        Background="Salmon"
                        Content="{Binding ErrorOutput}"
                        FontFamily="Consolas"
                        FontSize="10pt"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>
            </TabItem>
            <TabItem Header="Logs">
                <Grid VerticalAlignment="Stretch" Background="{ui:ThemeResource CardBackgroundFillColorDefaultBrush}">
                    <ScrollViewer>
                        <Label
                            x:Name="ScriptLogs"
                            Margin="5"
                            VerticalAlignment="Stretch"
                            Content="{Binding Logs}"
                            FontFamily="Consolas"
                            FontSize="10pt" />
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
