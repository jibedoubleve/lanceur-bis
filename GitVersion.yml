assembly-versioning-scheme: MajorMinorPatchTag
next-version: 2.8.0
branches:
  master:
    is-release-branch: true  
  develop:
    tag: beta
    increment: Patch
  feature:
    regex: ^issue?[\/-]
    tag: alpha-issue.{BranchName}
    increment: Patch

