assembly-versioning-scheme: MajorMinorPatchTag
next-version: 2.8.0
branches:
  feature:
    is-release-branch: true  
    regex: ^release?[\/-]    
  master:
    tag: beta
    increment: Patch
  feature:
    regex: ^issue?[\/-]
    tag: alpha-issue.{BranchName}
    increment: Patch

