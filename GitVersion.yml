next-version: 3.5.1
assembly-versioning-scheme: MajorMinorPatchTag
branches:

  master:
    tag: beta
    # increment: Minor

  feature:
    regex: ^release?[\/-]
    # increment: Minor

  feature:    
    regex: ^issue?[\/-]
    tag: alpha-issue.{BranchName}
    # increment: Minor
