next-version: 3.10.0
assembly-versioning-scheme: MajorMinorPatchTag
branches:

  master:
    tag: beta
    # increment: Minor

  feature:
    regex: ^release?[\/-]
    # increment: Minor

  feature:    
    regex: ^issue?[\/-]
    tag: alpha-issue.{BranchName}
    # increment: Minor
