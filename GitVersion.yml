next-version: 3.5.1
assembly-versioning-scheme: MajorMinorPatchTag
branches:

  master:
    increment: Minor
    tag: beta

  feature:
    increment: Minor
    regex: ^release?[\/-]

  feature:
    increment: Minor
    regex: ^issue?[\/-]
    tag: alpha-issue.{BranchName}
